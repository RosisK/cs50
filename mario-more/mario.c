#include <cs50.h>
#include <stdio.h>

int main(void)
{
    //Prompts user for height of pyramid
    int height = get_int("Height:");
    for(i = 0; i < %i; i++;), height
    {
        printf("#")
    }
}