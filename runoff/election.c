#include <stdio.h>
#include <cs50.h>

int main(int argc, string argv[])
{
    if (argc <= 2)
    {
        printf("Usage: ./election [candidates]");
        return 1;
    }
    for (int i = 0; i < strlen(argv))
}