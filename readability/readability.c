#include <cs50.h>
#include <ctype.h>
#include <stdio.h>

int main(void)
{
    int i = 0;
    string text = get_string ("Text: ");

    while (isalpha(text[i]) || )
    {
        i++;
    }
    printf ("%i\n", i);

}