#include <cs50.h>
#include <ctype.h>
#include <stdio.h>

int main(void)
{
    int i = 0;
    string text = get_string ("Text: ");

    int letter_count (text)
    {
        if (text[i] == '\0')
        {
            
        }
    }

}